# Anton's AI-Powered Resume Generator

This project generates a professional, high-design HTML resume from a simple Markdown file.

## 🚀 How to Update

1.  **Edit Content**: Modify `resume.md` with your latest experiences.
2.  **Generate HTML**: Run the build script:
    ```bash
    node scripts/build.js
    ```
3.  **Preview**: Open `index.html` in your browser.
4.  **Export**: Use the print button (bottom right) or `Ctrl + P` to save as PDF.

## 🛠 Project Structure
- `resume.md`: The source of truth for your content.
- `scripts/build.js`: The engine that converts MD to HTML.
- `index.html`: The final output (do not edit directly).

## ✨ Features
- **Auto-Formatting**: Automatically styles headers, lists, and dates.
- **Clean Design**: Optimized for readability and "Director-level" impact.
- **Print Ready**: CSS tuned for perfect PDF export.
